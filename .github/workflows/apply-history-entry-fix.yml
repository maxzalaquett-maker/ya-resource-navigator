name: Apply history entry snapshot fix

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - reopened
      - synchronize

permissions:
  contents: write

jobs:
  apply:
    if: github.head_ref == 'refactor/routing-history'
    runs-on: ubuntu-latest
    steps:
      - name: Check out routing branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Detect pending fix
        id: pending
        run: |
          if [ -f scripts/fix_history_entry_snapshot.py ]; then
            echo "apply=true" >> "$GITHUB_OUTPUT"
          else
            echo "apply=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Apply history entry fix
        if: steps.pending.outputs.apply == 'true'
        run: python scripts/fix_history_entry_snapshot.py

      - name: Remove one-time fix script
        if: steps.pending.outputs.apply == 'true'
        run: rm scripts/fix_history_entry_snapshot.py

      - name: Verify history entry fix
        if: steps.pending.outputs.apply == 'true'
        run: |
          node --check app.js
          node --check route-state.js
          node --check tests/e2e/smoke.spec.js
          npm run test:unit

      - name: Commit history entry fix
        if: steps.pending.outputs.apply == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Preserve filter state in browser history"
          git push origin HEAD:${{ github.head_ref }}
